import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import 'flowbite';
import "./Styles.css"; // ou seu arquivo principal de estilo

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);